{
  "name": "ruko",
  "version": "0.7.0",
  "description": "Ruko — AI Coding Agent CLI: connection-testing setup wizard, provider profiles, layered roles, plan mode, undo journal, loop detection, token caps, streaming replies, visual diffs, status-bar UI, session persistence in Node.js/TypeScript.",
  "type": "module",
  "bin": {
    "ruko": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test \"dist/tests/*.test.js\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "cli",
    "agent",
    "ai",
    "terminal",
    "coding-agent"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
